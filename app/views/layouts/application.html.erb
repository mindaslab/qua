<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Qua</title>
    <%= render partial: 'questions/header_boo' %>
    <!-- Le fav and touch icons -->
    <link href="images/favicon.ico" rel="shortcut icon">
    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
  </head>
  <body>

    <%= render partial: 'questions/nav_bar' %>

    <div class="container-fluid">
      <div class="row-fluid">
        <div class="span3">
          <div class="well sidebar-nav">
            <div class="tag_search"><%= text_field_tag :ts, nil, placeholder:"Search Tags", class: "eighty-percent" %></div>
            <ul id="tag_list" class="nav nav-list">
              <%= render partial: 'questions/tag', collection: Tag.order(:name).limit(50) %>
            </ul>
          </div><!--/.well -->
        </div><!--/span-->
        <div class="span9">
        <% if notice %>
          <div class="alert fade in">
            <a href="#" data-dismiss="alert" class="close">×</a>
            <ul>
              <%= raw notice %>
            </ul>
          </div>
        <% end %>
          <%= yield %>
        </div>
      </div><!--/row-->

      <%= render partial: 'questions/footer' %>

    </div> <!-- /container -->

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= render partial: 'questions/footer_tags' %>

  </body>
</html>
